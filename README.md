# TeamMate_University_Gaming_Club
TeamMate — automatic team formation for university gaming club (optional).
git pull origin main --allow-unrelated-histories
# resolve conflicts if prompted, then:
git add .
git commit -m "Merge remote README and local project"
git push origin main
